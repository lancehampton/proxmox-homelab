services:
  whatsupdocker:
    image: getwud/wud:8.1
    container_name: wud
    restart: unless-stopped
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    ports:
      - 3090:3000
    env_file: .env